const colores = "Amarillos rojo verdes azules grises blancos negro";
const patron = //;

const color = colores.match(patron);

color.forEach(element => {
    mostrar.innerHTML += element + "<br>";
});